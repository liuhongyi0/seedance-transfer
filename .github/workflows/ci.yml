name: CI — Security & Lint

on:
  push:
    branches: [main]
    paths:
      - 'Seedance/backend/**'
      - 'comfyui-seedance-wizard/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'Seedance/backend/**'
      - 'comfyui-seedance-wizard/**'

jobs:
  bandit:
    name: bandit — Python Security Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install bandit
      - name: bandit (Seedance backend)
        run: bandit -r Seedance/backend/ -x ./test_api.py,./test_e2e.py,./test_evolink.py,./test_flow.py -f txt || true
      - name: bandit (ComfyUI plugin)
        run: bandit comfyui-seedance-wizard/nodes.py -f txt || true

  semgrep:
    name: semgrep — SAST
    runs-on: ubuntu-latest
    container:
      image: returntocorp/semgrep:latest
    steps:
      - uses: actions/checkout@v4
      - name: semgrep (Python + Secrets + OWASP)
        run: |
          semgrep scan \
            --config "p/python" \
            --config "p/secrets" \
            --config "p/owasp-top-ten" \
            --config "p/cwe-top-25" \
            --include "Seedance/backend/**/*.py" \
            --include "comfyui-seedance-wizard/**/*.py" \
            --no-error || true

  ruff:
    name: ruff — Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          src: 'Seedance/backend'
          args: 'check --select E,F,N,S,B,PLC,PLE,ISC,RUF --ignore S101,S104,S603,S607,S608,B904,B007,RUF001,RUF002,RUF003,RUF006,RUF010,RUF100,N802,N806,PLC0415,E501,B006,B008'
